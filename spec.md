# 개발 환경 진단 펫 — 실행 스펙

> 2026 오픈소스 개발자대회 출품작 실행 스펙
> 작성일: 2026-08-19 / 출품작 제출 마감: 2026-08-27 18:00 (D-8)
> 이 문서는 인터뷰로 확정된 결정 사항이며, 여기 적힌 항목은 재논의 대상이 아니다.

---

## 0. 한 문장 정의

여러 개발 도구의 개별 진단(doctor) 결과를 하나로 모으고, 문제가 있으면 **정확히 어느 파일 몇 번째 줄을 어떤 명령으로 고치는지**를 안내하며, 화면 위에 투명한 창으로 직접 상주하는 데스크톱 펫.

---

## 0-B. 문제 정의와 차별점 (결과보고서 원재료)

이 절은 결과보고서의 **개발배경 및 목적**, **혁신성 및 차별성**, **기대효과** 항목에 그대로 쓰기 위한 원재료다. D4에 여기서 옮겨 쓴다(§14).

### 푸는 문제 (우선순위 순)

1. **진단이 원인을 짚어주지 않는다.** `flutter doctor` 같은 도구는 "무엇이 문제인지"는 말해도 **어느 파일 몇 번째 줄을 고쳐야 하는지**는 알려주지 않는다. → 증상 → **원인 파일:줄 번호** → 고치는 명령어로 번역한다.
2. **진단 도구가 흩어져 있고 존재조차 모른다.** `brew doctor`·`flutter doctor`·`npm doctor`가 각자 따로 있고 많은 개발자가 있는 줄도 모른다. → 설치된 도구를 자동 감지해 관련 doctor를 모두 돌리고 **하나의 헬스 스코어로 통합**한다.
3. **환경은 서서히 망가진다.** PATH 충돌, 죽은 alias, 고아 환경변수가 쌓이며 조용히 무너진다. **"works on my machine"의 실체가 이것**이다. → 마지막 정상 스냅샷과 비교해 **드리프트를 즉시 감지**한다(§6).
4. **(2차 서사) 코딩 에이전트가 막히는 이유의 상당수는 코드 버그가 아니라 환경 문제다.** 작업 전에 "이 프로젝트를 돌릴 준비가 됐는지" 미리 확인할 수 있다. → v1에서는 MCP 노출을 제외했으므로(§12) **보고서의 "향후 확장성 및 기대효과"에서 다룬다.**

### 차별점 — 선언적 적용 도구가 아니라 진단 도구다

**이 구분이 차별성 항목의 핵심이므로 보고서와 영상에서 명확히 한다.**

| 부류 | 대표 | 하는 일 | 우리와의 관계 |
|---|---|---|---|
| **선언적 적용** | `chezmoi`, `dotbot`, `donnemartin/dev-setup` | 미리 정의한 설정을 **적용**한다 | **다른 범주다.** 이들은 "이렇게 만들어라"를 실행할 뿐, **지금 상태가 정상인지 판단하지 않는다** |
| **개별 진단** | `brew doctor`, `flutter doctor`, `shellrc-doctor` | 자기 영역만 진단한다 | **결합 대상.** 어댑터로 흡수·통합한다 |
| **통합 진단(우리)** | — | 여러 도구의 진단을 모으고 원인을 지목하며 **시간에 따른 변화를 추적**한다 | 검색상 **선례 없음** (단, 제출 전 한 번 더 재확인 권장) |

**핵심 주장: 새 기술을 발명하지 않는다.** 이미 흩어져 있는 개별 진단 능력을 어댑터로 결합하는 것 자체가 가치다. 이 서사가 OSS 적절성(2-5) 항목의 근거가 된다.

---

## 1. 심사 일정과 그 함의 (신규 확인)

| 시점 | 단계 | 내용 | 우리 대응 |
|---|---|---|---|
| 8/27 18:00 | 출품작 제출 | 결과보고서 + **시연영상 3분** + 소스코드 | 아래 타임라인 |
| 9/3 | 1차 평가 | 서면 평가 (결과보고서 등) | 보고서 서사 = README 서사 일치 |
| **10/12~28** | **2차 평가** | **출품작 기능테스트 + 라이선스 검증** | 아래 §9 |
| 11/4 | 최종 평가 | 발표 평가 + 종합 평가 | 제출 후 준비 |

**함의 1 — 라이브 데모 리스크가 없다.** 8/27 제출 이후 첫 관문인 9/3 1차는 **서면 평가(결과보고서 등)**이며, 시연영상은 제출물의 일부로서 함께 검토된다. 즉 이 단계에 현장 실행이 전혀 없고 영상은 몇 번이든 재촬영할 수 있다. 이전 세션에서 "GUI 앱은 데모 안정성 점수가 낮다"고 우려한 것은 **1차에서는 해당되지 않으며, 오히려 영상 매체에서 온스크린 펫이 터미널 텍스트보다 훨씬 유리하다.**

**함의 2 — 10월 기능테스트는 심사위원이 직접 설치해서 돌린다.** 서명 없는 `.dmg`는 macOS Gatekeeper에 막혀 그 자체로 기능테스트 실패가 된다. → **소스에서 빌드해 실행하는 경로를 1급 시민으로 문서화**하고, `.dmg`는 우회 방법과 함께 보조로 제공.

**우선순위 명시:** `pnpm install && pnpm dev` 로 실행하는 경로가 **주 경로**이며 README 최상단에 온다. `.dmg`는 시간이 남을 때만 만드는 **보조 산출물**이고, 일정이 밀리면 가장 먼저 버린다(§14 포기 우선순위). 서명되지 않은 `.dmg` 하나에 기능테스트의 성패를 걸지 않는다.

**함의 3 — 라이선스 검증이 독립 심사 항목이다.** 소스코드는 8/27에 고정 제출되므로 **라이선스 문서는 8/27 안에 완결**해야 한다 (§9).

---

## 2. 최우선 결정: 3D vs 2D → **2D 확정 (렌더러 교체 가능하게 설계)**

**결정: 2D 스프라이트로 출품. 3D는 1차 합격 후 또는 대회 이후.**

근거:
- 팀이 1인이고, 코딩은 전량 에이전트에 위임하며, **사람의 이해가 병목**이다. three.js 씬 그래프·애니메이션 믹서·투명 배경 렌더링은 병목 지점에서 가장 비싼 코드다.
- 사용자 본인이 "수상이 우선, 3D는 되면 좋고"로 우선순위를 명시했다.
- 루브릭은 2D/3D를 **전혀 평가하지 않는다.** 3D에 쓰는 시간은 점수로 환산되지 않는다.

**완화 장치 — 렌더러 격리:** 펫 렌더링을 `<PetView state={...} />` 컴포넌트 **하나**로 격리한다. 이 컴포넌트의 계약은 "펫 상태(idle/thinking/worried/alarmed)를 받아 그린다"가 전부이고, 바깥 코드는 스프라이트인지 3D인지 알지 못한다. 3D 전환 시 이 파일만 교체하면 되며, 지금 이 설계에 드는 추가 비용은 사실상 0이다.

**애셋 조달:** MIT/CC0 기존 스프라이트 재사용 → 1차 합격 시 자체 캐릭터로 교체.

**애셋 출처 정책 (§3의 OpenPets 기각과 혼동 금지):** §3에서 기각한 것은 **OpenPets의 플러그인 SDK 경로**이지 애셋이 아니다. OpenPets는 MIT이므로 그 스프라이트 애셋을 가져다 쓰는 것은 코드 경로와 무관하게 자유롭다. 다만 **어느 애셋을 실제로 채택했는지가 라이선스 문서(§9)에 그대로 반영되어야 하므로, D4에 애셋을 고르는 즉시 출처를 기록한다.** 후보 우선순위는 ① itch.io 등의 CC0 픽셀 펫 스프라이트(고지 부담이 가장 가벼움) ② OpenPets 애셋(MIT, 저작권 고지 필요). 어느 쪽이든 원저작자·출처 URL·라이선스를 `THIRD-PARTY-NOTICES.md`에 기재한다.

---

## 3. Build vs Plug-in → **Build(A) 확정**

**결정: OpenPets 플러그인 경로를 폐기하고 Electron 앱을 직접 만든다.**

이전 세션에서 "미확인"으로 남긴 두 가지를 저장소에서 직접 검증한 결과, **둘 다 부정**으로 판명되었다.

검증 출처: `alvinunreal/openpets` — `packages/sdk/src/index.ts` (SDK의 공식 계약 파일), `docs/plugins.md`, `docs/sdk.md`, `docs/pet-format.mdx`

| 우리 핵심 기능 | OpenPets SDK v3 실태 | 판정 |
|---|---|---|
| `brew doctor` 등 셸 명령 실행 | **API 자체가 존재하지 않음.** `ctx.system`은 `info()`(platform/locale/timezone/theme/appVersion/online), `metrics()`(CPU/메모리/배터리), `openExternal()`, 클립보드가 전부 | ❌ 불가 |
| `.zshrc` 등 파일 읽기 | `ctx.files`는 `pick()`/`save()`뿐 — **OS 파일 다이얼로그로 사용자가 매번 직접 고른 파일만** | ❌ 불가 |
| 설정 파일 변경 감시 | `docs/sdk.md`에 "no ambient filesystem watching" 명시 | ❌ 불가 |
| 3D 커스텀 렌더러 | `pet-format`은 스프라이트/애니메이션 **애셋 기술 전용**. "Pet packages should describe assets; they should not require arbitrary code execution" 명시 | ❌ 불가 |

즉 **3D 여부와 무관하게** 진단 어댑터라는 우리 핵심 기능이 플러그인 샌드박스 안에서 아예 돌지 않는다.

**검토했으나 기각한 우회로:** `network:local` 권한으로 localhost HTTP 호출이 가능하므로, 진단 엔진을 별도 로컬 데몬으로 띄우고 플러그인이 호출하는 구조가 이론상 가능하다. 그러나 사용자가 데몬과 펫 앱을 따로 설치해야 하고 **배포 타깃이 3개가 되어 완주 리스크(G5)가 급등한다**(§4 근거 정정 참조). 기각.

**참고:** OpenPets 저장소 자체는 건강하다 (MIT, ★1,078, 2026-08-17 최신 커밋, 문서화 수준 매우 높음). 우리 용도에 맞지 않을 뿐이며, 발표에서 "실사 후 기각한 대안"으로 언급하면 기술 판단력의 근거가 된다.

---

## 4. 기술 스택 → **Electron + TypeScript + React 확정**

**Tauri 탈락 근거:** 프론트를 React로 짜도 백엔드가 Rust다. 사람의 이해가 병목인 구조에서 **리뷰 불가능한 코드 구간**이 생기는 것은 치명적이다. 팀의 검증된 경험은 JavaScript/React이므로 전 구간을 TypeScript로 통일한다.

### 저장소 구조 (배포 타깃 2개 엄수)

```
devenv-pet/                       # pnpm workspace, 단일 저장소
├─ packages/core/                 # [배포 타깃 1] 진단 엔진 — npm 라이브러리
│  ├─ src/host.ts                 #   주입 인터페이스 (테스트 전략의 핵심)
│  ├─ src/types.ts                #   Finding / Snapshot / HealthScore
│  ├─ src/adapters/               #   어댑터 5종
│  ├─ src/score.ts                #   헬스 스코어 산출
│  ├─ src/snapshot.ts             #   스냅샷 저장 · 드리프트 diff
│  ├─ src/cli.ts                  #   ⚠ 내부 개발/테스트 전용. 별도 제품으로 홍보 금지
│  └─ test/fixtures/              #   가짜 .zshrc, 가짜 doctor 출력 텍스트
└─ apps/pet/                      # [배포 타깃 2] Electron 데스크톱 펫
   ├─ main/window.ts              #   투명 · 항상 위 · 프레임 없는 창
   ├─ main/scheduler.ts           #   실행 시 1회 + 30분 + 절전해제
   ├─ renderer/PetView.tsx        #   ⭐ 렌더러 교체 지점 (2D→3D는 이 파일만)
   ├─ renderer/Bubble.tsx         #   말풍선
   └─ renderer/FixPanel.tsx       #   토글 패널 (§7)
```

**CLI 취급 주의:** `packages/core`의 CLI는 개발·테스트용 진입점일 뿐이다. README·보고서·영상 어디에서도 별도 제품으로 소개하지 않는다.

> **근거 정정:** 이전 초안은 이 규칙의 근거를 "루브릭 1-1: 배포 타깃 3개 이상 = 0점"으로 적었으나, **이는 오귀속이었다.** 아카이브 원문(`archive/research/03-tech-trends-2026.md`)을 확인한 결과 "3개 이상의 배포 타깃"은 채점 항목이 아니라 **2주 안에 완주하기 불리한 특성** 목록의 한 줄이다. 즉 0점 규칙이 아니라 **완주 리스크 요인**(게이트 G5)이다. 규칙 자체는 그대로 유지하되 — 1인·8일에 배포 타깃을 늘리면 완주가 위태롭다 — 발표·보고서에서 "0점 규칙"으로 설명해서는 안 된다.

### 핵심 타입 계약

```ts
// 주입 가능한 호스트 — 실환경은 NodeHost, 테스트는 FakeHost(fixture)
interface DiagnosticHost {
  exec(cmd: string, args: string[]): Promise<{ stdout: string; stderr: string; code: number }>;
  readFile(path: string): Promise<string | null>;
  env: Record<string, string | undefined>;
  homedir: string;
}

// 진단 결과 1건 — evidence가 우리의 핵심 차별점
interface Finding {
  id: string;                  // 안정적 식별자, 드리프트 diff의 키
  adapter: string;
  severity: 'ok' | 'warn' | 'error';
  title: string;               // 증상
  cause: string;               // 원인 설명
  // ⭐ "어느 파일 몇 번째 줄" — 차별점 1의 구현체.
  // 파일 기반 어댑터(shell-rc, version-manager)에서는 필수.
  // 가리킬 파일이 없는 문제(docker 데몬 미실행 등)에서만 생략 가능.
  evidence?: {
    file: string;
    line: number;
    excerpt: string;
  };
  fix: {
    description: string;
    command?: string;          // 실행 가능한 수정 명령 (토글 패널이 이걸 실행)
    manual?: string;           // 자동화 불가 시 수동 안내
    needsSudo?: boolean;       // true면 토글 자동 실행 대상에서 제외 (§7 안전장치)
    revert?: string;           // 되돌리기 명령. 없으면 되돌리기 버튼 비활성
  };
  reference?: string;          // 골든셋 출처 URL — 근거 있는 진단임을 증명
}
```

**`evidence` 원칙 (범위 한정):** **설정 파일에서 비롯된 문제는 `evidence`를 반드시 포함한다.** "무엇이 문제인지는 말해도 어디를 고칠지는 안 알려준다"가 우리가 공격하는 지점이므로, 우리 스스로 그 실수를 하면 안 된다.

다만 **가리킬 파일·줄이 애초에 존재하지 않는 문제**(예: Docker 데몬 미실행, git 전역 `user.email` 미설정)에는 이 원칙을 적용할 수 없다. 이 경우 `evidence` 대신 **`fix.command`를 반드시 채운다** — 사용자가 다음에 무엇을 해야 하는지가 항상 구체적이어야 한다는 것이 원칙의 본질이고, `evidence`는 그것을 파일 문제에서 구현하는 수단일 뿐이다.

따라서 실제 계약은 다음과 같다:

> 모든 Finding은 **`evidence` 또는 `fix.command` 중 최소 하나**를 가진다. 파일 기반 어댑터(shell-rc, version-manager)의 Finding은 `evidence`가 필수다.

이 규칙은 어댑터 단위 테스트로 강제한다(각 어댑터의 모든 Finding이 규칙을 만족하는지 검사).

---

## 5. 어댑터 v1 — 균형형 5개 확정

`자체형`은 우리가 직접 분석하는 것(차별점), `래핑형`은 기존 doctor 출력을 파싱해 번역하는 것(통합 서사).

| # | 어댑터 | 유형 | 체크 항목 |
|---|---|---|---|
| 1 | **shell-rc** | 자체 (포팅) | `.zshrc`/`.bashrc`/`.zprofile`의 ① 중복 PATH 엔트리 ② 존재하지 않는 경로를 가리키는 PATH ③ 대상이 없는 죽은 alias ④ 없는 파일을 `source`하는 줄 ⑤ 중복 alias — **모두 파일:줄 번호와 원문 발췌 포함**. `shellrc-doctor` 로직을 TypeScript로 포팅 (아래 상세) |
| 2 | **version-manager** | 자체 (고유) | ① nvm/pyenv shim이 PATH에서 시스템 바이너리에 밀리는 우선순위 충돌 ② **`.nvmrc`/`.python-version`과 실제 활성 버전 불일치** ③ 버전 매니저 초기화 줄이 rc 파일에 아예 없거나 잘못된 위치에 있음 — ②가 이 어댑터의 고유 가치다(아래 상세) |
| 3 | **homebrew** | 래핑 | `brew doctor` 출력을 파싱해 **파일:줄 단위 수정 지침으로 번역**. 미설치 시 정상 skip |
| 4 | **git** | 래핑 | ① 전역 `user.name`/`user.email` 누락 ② SSH 키 부재 또는 권한 오류(600 아님) ③ `~/.gitconfig` 문법 오류 |
| 5 | **docker** | 래핑 | ① CLI는 있는데 데몬 미실행 ② 엔진 자체 미설치 ③ docker context 오설정 |

1·2번이 **"증상→파일:줄→고치는 명령"** 차별점을 지고, 3·4·5번이 **"흩어진 doctor를 하나의 헬스 스코어로"** 통합 서사를 진다.

### 어댑터 1 — `shellrc-doctor` 로직 포팅 (확정)

**결정: 의존성으로 호출하지 않고, 로직을 TypeScript로 포팅한다.**

실물 확인 결과 (`github.com/nord342/shellrc-doctor`):

| 항목 | 실제 |
|---|---|
| 언어·규모 | **Python 단일 파일** `shellrc_doctor.py` (약 20KB) |
| 라이선스 | **MIT** — 수정·재배포 자유, 저작권 고지만 유지 |
| 인지도·활성도 | **★0**, 최근 커밋 2026-05-11 (정체) |
| 출력 | `--json` 옵션 **이미 존재**, `.zshrc:42` 형태로 **파일:줄 번호 제공** |
| 기능 범위 | 중복/죽은/미사용 alias, shadowed PATH, `source` 별 타이밍, 셸 시작 시간, **nvm+asdf·pyenv+asdf 충돌** |

**포팅을 택한 이유:**

1. **설치 의존성 0.** 의존성으로 호출하면 사용자가 Python과 이 도구를 따로 설치해야 하는데, macOS는 python3를 기본 제공하지 않는다. **10월 기능테스트에서 심사위원이 설치하지 않으면 가장 중요한 어댑터가 통째로 침묵한다.** 감수할 이유가 없는 리스크다.
2. **핵심 차별점을 남에게 인질로 주지 않는다.** "파일:줄 지목"이 우리 정체성인데 그 출력이 남의 스키마 변경에 종속되면 안 된다.
3. **결합 서사의 가치가 낮다.** ★0에 3개월 정체된 프로젝트와의 연동은 "기존 OSS 결합"의 설득력이 약하다. 반면 **MIT 로직을 정당하게 포팅하고 크레딧을 명시하는 것**은 오픈소스 위생으로 읽힌다.
4. 단일 파일 20KB라 포팅 자체가 현실적인 규모다.

**의무 사항 (반드시 지킬 것):**

- `THIRD-PARTY-NOTICES.md`에 원저작자·저장소 URL·MIT 전문 기재 (§9)
- 포팅한 소스 파일 상단에 유래를 주석으로 명시
- **README와 결과보고서에 "이 어댑터는 `shellrc-doctor`(MIT)의 진단 로직에서 유래했다"고 명시한다.** 숨기면 표절이고 밝히면 성실함이다. 반드시 밝힌다.

**⚠️ 어댑터 2와의 중복 정리:** `shellrc-doctor`는 `nvm + asdf`, `pyenv + asdf` **충돌 감지를 이미 포함**한다. 포팅하면 그 기능이 어댑터 1로 들어오므로, **어댑터 2는 그 부분을 다시 구현하지 않는다.**

- **어댑터 1 (포팅분)**: rc 파일 *안에서* 발견되는 버전 매니저 설정 충돌
- **어댑터 2 (우리 고유)**: rc 파일 밖의 **런타임 상태 불일치** — `.nvmrc`/`.python-version`이 요구하는 버전과 **지금 실제로 활성화된 버전이 다른 것**. 파일만 읽어서는 알 수 없고 실제 버전을 조회해야 하며, `shellrc-doctor`에 없는 기능이다.

이 구분이 흐려지면 어댑터 2가 "포팅한 걸 또 만든 것"이 되어 차별점이 사라진다.

**타깃 OS:** 어댑터 구현과 시연영상은 **macOS 전용**. 코어 엔진 코드는 플랫폼 분기를 열어두되(`host.platform`), Linux/Windows 대응은 v1 범위 밖으로 명시한다. 1인·8일에 크로스플랫폼 검증은 불가능하며, 되지도 않는 걸 된다고 쓰는 게 기능테스트에서 더 위험하다.

**⚠️ 비-macOS 심사위원 대응 (10월 기능테스트 대비):** 심사위원이 어느 OS를 쓸지 우리는 통제할 수 없다. macOS가 아닌 환경에서 앱을 열었을 때 **오류로 죽거나 빈 화면이 나오면 그 자체로 기능테스트 실패**다. 다음 3개를 최소 요건으로 한다.

1. **앱은 어느 OS에서도 정상 기동한다.** 지원하지 않는 플랫폼에서는 펫이 뜨고, 패널에 *"v1은 macOS 어댑터만 제공합니다. 현재 플랫폼: Linux"* 를 명확히 표시한다. 크래시·무응답은 허용하지 않는다.
2. **fixture 기반 테스트는 모든 OS에서 돈다.** 실제 OS 상태에 의존하지 않으므로(§8) 심사위원이 어디서든 `pnpm test`로 진단 로직의 정확성을 검증할 수 있다. **이것이 비-macOS 심사위원에게 우리 엔진을 증명하는 주 경로다.**
3. **README 최상단에 지원 플랫폼을 명시한다.** 숨기지 않는다. 범위를 명확히 선언한 것과 그냥 안 되는 것은 심사에서 전혀 다르게 읽힌다.

### 헬스 스코어

100점에서 시작해 Finding 하나당 차감한다. **배점은 다음으로 고정한다** — 영상·보고서에 점수를 띄우는 이상 숫자가 매번 달라지면 안 된다.

| 항목 | 값 |
|---|---|
| `error` 1건 | −15 |
| `warn` 1건 | −5 |
| **어댑터 1개의 최대 감점** | **−30** (한 어댑터가 문제를 쏟아내도 점수 전체를 잠식하지 못하게) |
| 하한 | 0점 |

**skip 처리:** 해당 도구가 미설치라 진단이 불가능한 어댑터는 감점도 없고 분모에도 넣지 않는다 — Docker를 안 쓰는 사람이 감점당하면 안 된다. UI에는 "해당 없음"으로 별도 표기해, 점수가 높은 이유가 "문제가 없어서"인지 "검사를 안 해서"인지 사용자가 구분할 수 있게 한다.

**등급 표시:** 90+ 정상 / 70–89 주의 / 70 미만 문제 있음. 펫 상태(§7)와 연동한다.

---

## 6. 드리프트 감지 (핵심 가치)

- **스냅샷 저장 위치:** `~/.devenv-pet/snapshots/latest.json` (Electron `app.getPath('userData')` 대신 홈 디렉터리 고정 — 사용자가 직접 열어볼 수 있어야 신뢰가 생김)
- **저장 단위 — 어댑터별로 분리한다 (중요):** 스냅샷은 전체를 한 덩어리로 저장하지 않고 **어댑터별 레코드**로 저장한다. 각 레코드는 `{ adapter, ranAt, findings[] }`를 가진다.

```jsonc
{
  "shell-rc":        { "ranAt": "2026-08-19T10:30:00Z", "findings": [/* ... */] },
  "version-manager": { "ranAt": "2026-08-19T10:30:00Z", "findings": [] },
  "homebrew":        { "ranAt": "2026-08-19T09:00:00Z", "findings": [/* ... */] }
}
```

- **비교 방식 — 이번에 실제로 돌린 어댑터 범위 안에서만 diff한다 (중요):** Finding의 `id` 집합을 비교하되, **이번 실행에 포함되지 않은 어댑터의 레코드는 손대지 않고 그대로 유지**한다. **이전에 없던 `error`가 새로 생기면** = 드리프트 발생 → 펫이 즉시 `alarmed` 상태로 전환하고 말풍선을 띄운다.

> ⚠️ **이 두 규칙은 선택이 아니다.** 30분 주기 체크는 자체형 어댑터만 돌리므로(아래 부하 관리), 전체 `id` 집합을 통째로 diff하면 그때마다 래핑형 Finding이 **"사라졌다"로 오판**되고 다음 전체 스캔에서 다시 **"새로 생겼다"로 오판**된다. 펫이 30분마다 헛되이 놀라는 앱이 된다. 어댑터별 분리 저장 + 실행 범위 한정 diff가 이걸 막는다.
- **트리거 (확정):** ① 앱 실행 시 1회 ② 30분마다 ③ 절전 해제 시 (`powerMonitor.on('resume')`)
- **부하 관리:** 30분 주기 체크는 **자체형 어댑터(shell-rc, version-manager)만** 돌린다 — 파일 읽기뿐이라 사실상 무료다. 무거운 래핑형(`brew doctor` 등)은 **절전 해제 시와 수동 실행 시에만** 돈다. 이 구분이 "상시 감지"와 "배터리 부담 없음"을 동시에 만족시킨다.

---

## 7. 온스크린 펫 UI/UX

### 창

투명 배경 · 항상 위 · 프레임 없음 · 작업 표시줄 미노출 · 드래그로 이동 가능. 클릭 관통(`setIgnoreMouseEvents`)은 펫 영역 밖에만 적용해 작업을 방해하지 않는다.

### 캐릭터 상태 4종

| 상태 | 조건 | 연출 |
|---|---|---|
| `idle` | 모든 어댑터 정상 | 평온하게 앉아있거나 천천히 돌아다님 |
| `thinking` | 진단 실행 중 | 고민하는 모션 |
| `worried` | `warn` 있음 | 걱정하는 모션, 작은 느낌표 |
| `alarmed` | `error` 있음 또는 **새 드리프트 감지** | 강한 반응 + 말풍선 자동 표시 |

### 클릭 시

말풍선(요약: 헬스 스코어 + 가장 심각한 문제 1건) → 클릭하면 상세 패널 확장.

### 상세 패널 = 토글 패널 (설계 결정 3의 구현)

문제 목록을 **스위치/토글 형태**로 표시한다. 각 항목은 파일 경로·줄 번호·원문 발췌·수정 명령을 보여준다.

사용자가 토글을 켜면:
1. 펫이 그 토글로 **걸어가서 직접 조작하는 애니메이션**을 수행한다 (시각적 재미)
2. 동시에 백그라운드에서 진단 엔진이 **실제 수정 명령을 실행**한다
3. 재진단 후 결과를 반영

이 패널은 **우리가 100% 통제하는 자체 UI**다. 실제 OS 설정 화면을 건드리지 않으므로 화면 자동화가 전혀 없고, OS 버전에 따라 깨질 여지도 없다. 사용자에게는 "펫이 직접 고쳐줬다"로 보이지만 구조적으로 완전히 안전하다.

### 안전장치 (10월 기능테스트에서 심사위원이 자기 머신에서 돌린다 — 선택이 아니라 필수)

1. **실행 전 확인.** 토글을 켜면 곧바로 실행하지 않는다. **실행할 명령 전문을 그대로 보여주고** 사용자가 확인해야 진행한다. 사용자가 무엇이 실행되는지 모르는 채로 시스템이 바뀌는 일은 없어야 한다.
2. **파일 수정은 `.bak` 백업 후.** 원본을 `파일명.bak.<타임스탬프>`로 남기고, 패널의 "되돌리기"로 복원할 수 있게 한다.
3. **sudo가 필요한 명령은 자동 실행하지 않는다.** `fix.needsSudo`가 `true`면 토글을 **비활성**으로 표시하고, 대신 **명령을 복사할 수 있게만** 제공한다. 사용자가 직접 터미널에서 실행하도록 한다. 권한 상승을 앱이 대신 하지 않는다.
4. **되돌릴 수 없는 fix는 되돌릴 수 없다고 표시한다.** `fix.revert`가 없는 항목(예: 패키지 재설치류)은 되돌리기 버튼을 비활성화하고 **실행 전 확인 단계에서 그 사실을 명시**한다. 되돌릴 수 있는 척하는 게 못 되돌리는 것보다 나쁘다.
5. **파괴적 작업은 v1에서 아예 제외한다.** 파일·패키지 삭제, 전역 설정 초기화 같은 fix는 어댑터에 넣지 않는다. v1의 fix는 **설정 추가·수정과 서비스 시작** 수준으로 한정한다.

---

## 8. 테스트 전략 — 주입 인터페이스 + 텍스트 fixture

모든 어댑터는 `DiagnosticHost`를 주입받아 동작한다. 실제 파일시스템이나 셸에 직접 접근하는 코드는 어댑터 안에 **한 줄도 없다.**

- 실환경: `NodeHost` — 진짜 `child_process`/`fs`
- 테스트: `FakeHost` — `test/fixtures/`의 텍스트를 반환

fixture 예시:
```
test/fixtures/
├─ zshrc/duplicate-path.zshrc
├─ zshrc/dead-alias.zshrc
├─ zshrc/missing-source.zshrc
├─ brew-doctor/outdated-formula.txt
└─ docker/daemon-not-running.stderr.txt
```

각 fixture는 골든셋의 실제 사례 1건에서 유래하며, 테스트 파일에 출처 URL을 주석으로 남긴다. **"실제로 보고된 문제를 재현하는 테스트"**라는 사실 자체가 루브릭 2-6(기능테스트)의 근거가 된다.

Docker 컨테이너 기반 통합 테스트는 v1 범위 밖이다 — macOS 고유 이슈(Homebrew 등)는 컨테이너로 재현되지 않아 비용 대비 효용이 낮다. 시간이 남으면 리눅스에서도 도는 어댑터에 한해 추가한다.

CI: GitHub Actions에서 `pnpm test` — 실제 OS 상태에 의존하지 않으므로 그대로 통과한다. **초록 배지가 README에 붙는 것 자체가 심사 자료다.**

---

## 9. 라이선스 대응 (10월 2차 평가 대비, 8/27까지 완결)

2차 평가에 **라이선스 검증**이 독립 항목으로 있고, 소스코드는 8/27에 고정 제출된다.

- [ ] 프로젝트 `LICENSE` — **MIT 권장.** 채택 예정 애셋(CC0 또는 MIT, §2)과 충돌하지 않고, 심사위원이 라이선스 검증에서 가장 빠르게 확인할 수 있다
- [ ] `THIRD-PARTY-NOTICES.md` — 모든 런타임 의존성의 라이선스 목록
- [ ] **`shellrc-doctor` 포팅 고지 (필수)** — 어댑터 1은 MIT 프로젝트의 로직을 포팅한 것이다. `THIRD-PARTY-NOTICES.md`에 원저작자·저장소 URL·MIT 전문을 기재하고, 소스 주석과 README에도 유래를 명시한다. **2차 라이선스 검증에서 가장 먼저 확인될 항목이다**
- [ ] **스프라이트 애셋 출처 고지** — MIT/CC0 애셋의 원저작자·출처 URL·라이선스 명시. 가장 놓치기 쉬운 항목이며 심사에서 바로 걸린다.
- [ ] 트랜지티브 의존성에 GPL/AGPL 혼입 여부 스캔 (`license-checker` 등으로 자동화하고 결과를 커밋)
- [ ] Electron 자체의 라이선스 고지 요건 확인

---

## 9-B. 제출물 요건 (공식 양식 확인 완료)

출처: `2026 오픈소스 개발자대회 결과보고서_접수번호(팀명).docx`

### 파일 구성

| 구분 | 항목 | 필수 | 분량 |
|---|---|---|---|
| 결과보고서 | 프로젝트 개요 및 세부 내용 | 필수 | **5페이지 이내** |
| 붙임1 | **SBOM (소프트웨어 자재명세서)** | **필수** | 제한 없음 |
| 붙임2 | AI 모델 활용 및 라이선스 기술 명세서 | 해당 시 | 제한 없음 |

- **총 2개 파일 제출:** DOCX(또는 HWP) 1부 + **동일 파일의 PDF 변환본** 1부
- 글꼴 맑은고딕 본문 10pt, **여백 설정 변경 금지**, 가이드 문구·안내 페이지 삭제 필수
- 파일명: `2026 오픈소스 개발자대회 결과보고서_접수번호(팀명)`
- 제출: osscontest.kr 온라인

### ⚠️ 시연영상은 유튜브 URL로 제출한다

**별도 파일 제출이 불가**하며 보고서에 URL을 기재한다. 따라서 **유튜브 업로드와 처리 시간까지 일정에 포함**해야 하며, 계정 준비도 미리 해둔다. 미공개(일부공개) 설정 여부는 제출 직전 확인한다.

### ⚠️ 프로젝트명은 상장에 그대로 새겨진다

양식 명시: *"수상 시 상장에 그대로 기재되므로, 특수문자를 제외한 핵심 키워드 중심의 프로젝트명 기재"*. **특수문자 불가.** 참가 접수 정보와 동일해야 하므로, 접수 시 기재한 이름을 먼저 확인한 뒤 확정한다.

### 붙임1 SBOM — 의존성을 늘리지 말아야 하는 진짜 이유

요구 항목: 번호 / 라이브러리명 / 버전 / **라이선스** / 공식 저장소 URL / **사용 목적 및 주요 기능**

의존성 하나가 곧 표의 한 줄이고, 각 줄마다 라이선스와 용도를 우리가 직접 적어야 한다. **§13의 "새 의존성 추가는 사전 승인" 규칙이 여기서 실제 비용으로 돌아온다.**

> **작성 규칙: 의존성을 추가하는 그 순간 SBOM 행도 함께 추가한다.** 마지막에 몰아서 쓰면 Electron 트리 전체를 역추적해야 한다. 직접 의존성만 기재하되, 판단이 애매하면 포함하는 쪽을 택한다.

### 붙임2 — 우리는 제출 대상이다 (판단 근거)

유형 1·2·3은 **프로젝트에 AI 모델을 탑재한 경우**를 뜻하므로 우리는 **모두 해당 없음**이다. 양식도 *"개발 과정에서 코딩 및 디버깅 보조용으로 상용 AI를 단순 활용한 경우는 체크하지 않음"*이라고 명시한다.

그러나 붙임2의 **4번 항목은 "모든 유형 필수 작성"**이며, 여기에 **`상용 AI 보조도구 활용 여부 및 범위`** 를 적게 되어 있다. 우리는 구현 코드를 사실상 전량 에이전트로 작성하므로 **붙임2를 제출하고 정직하게 기재한다.**

- 유형 체크: 1·2·3 모두 미체크 (탑재 모델 없음)
- 2·3번 항목: "해당 없음"
- 4번 항목 기재 방향: **범위를 축소해 적지 않는다.** 실제 분담(§13)을 그대로 쓴다 — 요구사항 정의·아키텍처 설계·스펙 확정·코드 리뷰 및 채택 판단은 참가자가 수행하고, 구현 코드 작성은 Claude Code를 활용했다는 사실을 명시한다.
- **대회 규정상 AI 보조도구 활용은 금지가 아니다.** 양식이 "체크하지 않고 4번에 기재하라"고 별도 항목을 둔 것 자체가 허용의 근거다. 축소 기재했다가 나중에 드러나는 것이 훨씬 큰 위험이다.

---

## 10. 골든셋 수집 계획

**목표: 25건.** 방식은 **공개 이슈·Q&A 채굴**로 확정.

- 출처: GitHub Issues, Stack Overflow, 반복적으로 인용되는 셋업 블로그
- 각 건마다: 증상 원문 / 원인 / 해결 방법 / **출처 URL** / 우리 어댑터 중 어디가 잡는지 / 해당 fixture 파일명
- 저장: `docs/goldenset.md` (표 형태)
- 부수 효과: 출처 URL이 남아 결과보고서에서 **"우리가 지어낸 문제가 아니라 실제로 보고된 문제"**임을 증명한다. 이게 활용성 점수의 근거다.
- **활용 — 측정 방법을 먼저 정의한다.** "X분 → Y분"을 근거 없이 쓰면 심사에서 바로 무너진다. 다음 두 지표만 쓰고, 산출 근거를 보고서에 그대로 적는다.

  | 지표 | 정의 | 측정 방법 |
  |---|---|---|
  | **감지율** | 골든셋 25건 중 우리 어댑터가 잡아내는 건수 | fixture 테스트로 자동 산출. **가장 방어 가능한 숫자이므로 이걸 주력으로 쓴다** |
  | **탐색 시간 절감** | 문제 원인을 찾기까지 걸린 시간 | **기존 방식** = 해당 사례의 원 출처(GitHub 이슈·SO 질문)에서 *질문 작성 시각 → 채택 답변 시각*을 기록. **우리 방식** = 앱 실행 후 해당 Finding이 화면에 뜨기까지의 실측 시간 |

  탐색 시간 절감은 표본이 적으므로 **평균 대신 중앙값**을 쓰고, "N=몇 건 기준"을 반드시 병기한다. 표본이 부족하면 이 지표는 버리고 감지율만 제시한다 — 없는 숫자를 만들지 않는다.

수집은 D1부터 매일 조금씩 분산한다. 하루에 몰아서 하면 반드시 밀린다.

---

## 11. 커뮤니티 접점

> **이전 계획 폐기:** 초안은 "`shellrc-doctor`에 JSON 출력 옵션을 요청하는 이슈"를 계획했으나, **실물 확인 결과 `--json`이 이미 구현되어 있어 무효**가 되었다. 확인 없이 올렸다면 상대의 README도 안 읽은 셈이 되어 역효과였을 것이다.

**확정: 실제로 써보고 발견한 진짜 문제로 이슈 1건 남기기.** (PR은 범위 밖)

- 대상: `github.com/nord342/shellrc-doctor`
- **전제 조건: 먼저 실제로 돌려본다.** D1에 포팅하면서 원본을 여러 fixture로 실행하게 되므로 그 과정에서 발견한 것을 쓴다. **없는 문제를 지어내지 않는다** — 이슈 1건이 없는 것보다 억지 이슈 1건이 더 나쁘다.
- 이슈 후보 (실제 발견된 것만 채택):
  - `--json` 출력에 **스키마 버전 필드 부재** — 소비하는 쪽에서 형식 변경을 감지할 방법이 없다. 우리가 포팅하며 실제로 겪는 문제다
  - 특정 셸 문법(다중 라인 `export`, 조건부 `source`, `eval` 안의 PATH 조작) 오탐/미탐
  - macOS 특유 경로(`/opt/homebrew` vs `/usr/local`) 처리 사례
- **이슈에 반드시 포함할 것:** 재현 가능한 최소 입력(fixture 내용), 기대 동작과 실제 동작, 우리 프로젝트 링크, **그리고 MIT 하에 로직을 TypeScript로 포팅했다는 사실과 감사 표시.** 크레딧을 이슈에서 먼저 밝히는 것이 가장 깔끔한 순서다.
- 시점: **D3(8/21)**. **★0에 3개월 정체된 저장소이므로 답변을 기대하지 않는다.** 이슈를 남긴 사실 자체가 증거이며, 이것이 이 계획의 리스크가 낮은 이유다.
- 산출물: 이슈 URL과 스크린샷을 결과보고서에 첨부

---

## 12. 범위 제외 확정

| 항목 | 판단 |
|---|---|
| MCP 서버 노출 | **v1 제외.** 보고서·영상에서 향후 계획으로만 언급 |
| 3D 캐릭터 | **v1 제외.** 렌더러 격리로 교체 가능하게만 설계 |
| Linux/Windows 어댑터 | **v1 제외.** 코드는 분기 가능하게, 지원은 macOS만 명시 |
| Docker 통합 테스트 | v1 제외 (시간 남으면 추가) |
| `shellrc-doctor` PR | v1 제외 (이슈만) |
| CLI 제품화 | **영구 제외.** 내부 개발용으로만 유지 |

---

## 13. 역할 분담 (1인 + 에이전트)

**전제: 코딩은 전량 에이전트에 위임. 사람은 결정권자이자 테이스트 담당. 따라서 병목은 "사람이 에이전트의 작업을 이해하는 속도"다.**

| 사람이 하는 일 | 에이전트가 하는 일 |
|---|---|
| 스코프 결정, 무엇을 뺄지 판단 | 구현 전량 |
| 어댑터별 "이게 진짜 문제인가" 판정 | 테스트 작성 |
| 골든셋 큐레이션 (수집은 위임, 채택은 사람) | 문서 초안 |
| 펫 캐릭터·UX 취향 결정 | 라이선스 스캔 자동화 |
| 영상 구성과 서사 | 보고서 초안 |

### 병목 완화 규칙 (반드시 지킬 것)

1. **하루에 한 덩어리씩.** 여러 모듈을 병렬로 진행하지 않는다. 이해가 병목인데 병렬로 벌리면 이해가 밀린다.
2. **각 덩어리 끝에 사람이 직접 실행한다.** 테스트를 돌려보고, 통과한 테스트 파일을 실제로 읽는다. 읽지 않고 넘어간 코드가 쌓이면 D7 이후 손을 못 댄다.
3. **새 의존성 추가는 사전 승인.** 에이전트가 임의로 라이브러리를 늘리지 않는다. 라이선스 검증(§9) 부담이 그대로 늘어난다.
4. **파일 수를 늘리지 않는다.** 추상화는 실제로 두 번째 사용처가 생겼을 때만 도입한다.
5. **이해가 안 되는 코드는 그 자리에서 되묻는다.** 넘어가지 않는다.

---

## 14. 일자별 타임라인 (8/19 수 ~ 8/27 목 18:00)

### 실제 가용 시간 (확정)

| 날짜 | 요일 | 가용 |
|---|---|---|
| 8/19 | 수 | **풀타임** |
| 8/20~8/23 | 목·금·토·일 | **각 2~3시간** (일정 있음) |
| 8/24~8/26 | 월·화·수 | **풀타임** |
| 8/27 | 목 | ~18:00 마감 |

**풀타임은 4일(8/19, 24, 25, 26)뿐이고 주말이 전부 저강도다.** 초안이 Electron 작업을 주말 풀타임에 배치했던 전제가 무너졌으므로 아래와 같이 재배치한다.

**재배치 원칙 3가지:**
1. **저강도 4일(8/20~23)에는 코딩량이 작고 독립적인 일을 넣는다** — 어댑터는 하나씩 끝나는 단위 작업이라 2~3시간에 맞고, 골든셋 채굴과 보고서 초고는 읽기·쓰기라 저강도 시간에 가장 잘 맞는다.
2. **큰 미지수는 첫날에 제거한다** — 투명·항상 위 창이 실제로 뜨는지가 이 프로젝트 최대의 기술 리스크다. 8/24에 처음 시도해서 실패하면 복구 시간이 없다. **D1에 스파이크로 먼저 확인한다.**
3. **보고서를 마지막에 몰지 않는다** — 5페이지 중 상당 부분(개발배경·아키텍처·차별성)은 이미 `spec.md`에 있다. 저강도 시간에 미리 옮겨 쓰고, 제품이 있어야 쓸 수 있는 부분만 남긴다.

### 일자별

| 일자 | 가용 | 작업 | 완료 판정 기준 |
|---|---|---|---|
| **D1 8/19(수)** | 풀 | ① **Electron 스파이크(최우선, 1~2h)** — 투명·프레임없는·항상 위 창에 스프라이트를 띄우고 드래그<br>② 워크스페이스 스캐폴딩 + `DiagnosticHost`·`Finding` 타입 확정<br>③ **어댑터 1(shell-rc) 포팅 착수** | 화면에 펫이 실제로 떠 있다. `pnpm test`가 돈다 |
| **D2 8/20(목)** | 2~3h | 어댑터 1 포팅 완료 + fixture 테스트 | 망가진 `.zshrc` fixture에서 **파일:줄이 정확히** 나옴 |
| **D3 8/21(금)** | 2~3h | 어댑터 2(version-manager) + **골든셋 채굴** | 어댑터 2 통과. 골든셋 10건 이상 |
| **D4 8/22(토)** | 2~3h | 어댑터 3(homebrew) + 어댑터 4(git) + **보고서 초고 착수**(개발배경·아키텍처·차별성을 spec에서 이관) | 어댑터 4개 통과. 보고서 3개 섹션 초안 |
| **D5 8/23(일)** | 2~3h | 어댑터 5(docker) + 헬스 스코어 + **`shellrc-doctor` 이슈 등록** + 골든셋 마무리 | 어댑터 5개 통과. 이슈 URL 확보. 골든셋 25건 |
| **D6 8/24(월)** | 풀 | 스냅샷 저장·드리프트 diff + **Electron 앱 본체** (창·`PetView`·드래그·IPC·스케줄러) | 펫이 진짜 진단 결과에 따라 상태를 바꾼다 |
| **D7 8/25(화)** | 풀 | 말풍선 + 토글 패널 + fix 실행 + **안전장치 5종(§7)**. **영상 씬 부분 촬영 시작** | 토글을 켜면 실제로 고쳐지고 재진단된다. 씬 절반 촬영 완료 |
| **D8 8/26(수)** | 풀 | **붙임1 SBOM** + 라이선스 문서(§9) + README + CI + 소스 빌드 검증<br>**영상 편집 완료 → 유튜브 업로드** | CI 초록. 유튜브 URL 확보. SBOM 표 완성 |
| **D9 8/27(목)** | ~18:00 | 보고서 마감(**5p 이내**) + **PDF 변환** + 붙임2 작성 + 최종 점검 + **제출** | 2개 파일 제출 완료 |

**D9은 작성·제출 전용이다.** 새 기능은 물론이고 영상 재촬영도 D9에 넣지 않는다 — 유튜브 업로드까지 걸리는 시간을 마감일에 감당할 수 없다.

### 포기 우선순위 (일정이 밀렸을 때 이 순서로 버린다)

1. `.dmg` 패키징 (§1 함의 2 — 소스 빌드가 주 경로)
2. 어댑터 5 (docker) — 가장 단순하고 서사 기여도 낮음
3. 펫이 토글로 걸어가는 애니메이션 — 토글 기능만 남기고 생략
4. 어댑터 4 (git)
5. **절대 못 버리는 것:** 어댑터 1·2(차별점), 드리프트 감지(핵심 가치), **영상·보고서·SBOM**(제출 필수)

> 제출물 3종(영상·보고서·SBOM)은 **기능이 아니라 자격 요건**이다. 기능을 하나 더 넣으려다 이걸 놓치면 심사 자체가 성립하지 않는다.

## 15. 시연영상 3분 구성안

| 구간 | 내용 |
|---|---|
| 0:00–0:20 | **문제 제기.** `flutter doctor` 결과를 보여준다. "문제가 있다"고는 하는데 **어디를 고치라는 말이 없다** |
| 0:20–0:35 | 펫 등장. 화면 위에 상주하며 작업을 방해하지 않는 모습 |
| 0:35–1:15 | **통합.** 흩어져 있던 `brew doctor`/`git`/`docker` 등이 하나의 헬스 스코어로. "이런 게 있는 줄도 몰랐던" 진단들이 자동 수집됨 |
| 1:15–2:10 | **핵심 차별점.** 클릭 → 패널 → `.zshrc:47` 원문 발췌와 수정 명령이 그대로 보임. 토글을 켜면 펫이 조작하고 실제로 고쳐짐 |
| 2:10–2:45 | **드리프트 감지.** `.zshrc`를 일부러 망가뜨린다 → 펫이 스스로 알아채고 걱정 상태로 전환 → "어제까지 되던 게 오늘 안 됨"을 즉시 포착 |
| 2:45–3:00 | 마무리 — 기존 오픈소스를 결합했다는 점, 배포 타깃, 향후 계획(MCP·3D) |

> **제출 형식:** 영상은 **유튜브 업로드 후 URL만 보고서에 기재**한다. 파일 제출 불가(§9-B). 업로드·처리 시간을 D8 안에 확보한다.

### 촬영 안정성

- **각 씬을 개별 촬영**한다. 3분 원테이크를 노리지 않는다.
- **고장난 환경을 스크립트로 재현 가능하게** 만든다 (`scripts/demo-setup.sh`). 재촬영 시 같은 상태를 즉시 복원할 수 있어야 한다.
- 촬영은 D8이지만, **D5 시점부터 씬별로 미리 찍어둔다.** 완성 후 한 번에 찍으면 재촬영 여유가 없다.

---

## 16. 확인 필요 (사용자 액션)

- [x] ~~결과보고서 양식 확인~~ → **완료.** §9-B에 반영 (5p 제한, SBOM 필수, PDF 동시 제출, 유튜브 URL)
- [x] ~~일정 있는 날 확정~~ → **완료.** 8/20~23 저강도, §14 타임라인 재배치 완료
- [ ] **프로젝트 이름 확정 (보류 중)** — 단, 접수 시 기재한 이름과 동일해야 하고 **상장에 그대로 새겨지며 특수문자 불가**(§9-B). 늦어도 D8까지는 확정할 것
- [ ] **유튜브 계정 준비** — 영상은 파일 제출 불가, URL만 받는다(§9-B). D8 업로드 전까지 준비
- [ ] **접수번호 확인** — 제출 파일명에 들어간다 (`결과보고서_접수번호(팀명)`)

---

## 부록 A: 심사 기준 참조 (외부 인용의 근거)

이 문서는 "루브릭 2-6", "게이트 G1" 같은 표현을 판단 근거로 반복 인용한다. **이 정의가 문서 안에 없으면 다음 세션이 `spec.md`만 읽고는 가장 중요한 기준을 검증할 수 없으므로** 여기에 옮겨 둔다.

**원본 위치:** `archive/research/00-official-criteria.md`, `archive/research/05-idea-scoring.md`, `archive/research/03-tech-trends-2026.md`, `archive/idea-review.html`
**정본은 아카이브다.** 아래는 참조 편의용 발췌이며, 충돌 시 아카이브 원문을 따른다.

### 배점 (총 100점)

| 항목 | 배점 | 항목 | 배점 |
|---|---|---|---|
| 1-1 구조·코드 완성도 | 6 | 2-1 작품발표 PT | 10 |
| 1-2 OSS 발전 가능성 | 6 | **2-2 활용성** | **15** |
| 1-3 개발 문서 구체성 | 6 | 2-3 데모 완성도 | 10 |
| 1-4 혁신성 | 6 | 2-4 커뮤니티 확장 | 10 |
| 1-5 팀워크 | 6 | 2-5 OSS 적절성 | 10 |
| | | 2-6 기능테스트 | 10 |
| **소계** | **30** | 2-7 라이선스 검증 | 5 |
| | | **소계** | **70** |

**활용성(15점)이 단일 최대 항목이다.** 골든셋(§10)에 투자하는 이유가 이것이다.

### 하드 게이트

| 게이트 | 내용 | 우리 상태 |
|---|---|---|
| G1 | 소비자 서비스 금지 | 통과 — 스코프를 "개발 환경/개발 도구"로 좁게 유지하는 한 |
| G2 | 레드오션 금지 (개인비서 등 15종 명시) | 통과 — "doctor 통합 aggregator"는 부재 확인 |
| G3 | 상용 API 종속 금지 | 통과 — 로컬 실행, 외부 API 없음 (MCP·LLM 호출을 핵심 메커니즘으로 넣지 않는 이유) |
| G4 | 라이선스 문제 | 통과 예정 — §9로 관리 |
| G5 | **완주 리스크** | **최대 리스크.** 1인·8일. 배포 타깃을 2개로 묶는 진짜 이유가 이것이다 |
| G6 | 정확성 증명 가능성 | 통과 — fixture 테스트로 감지율을 숫자로 제시 가능(§10) |
| G7 | 학습/알고리즘 정확성 증명 불가 | 해당 없음 — 모델 학습이 크리티컬 패스에 없음 |
| G8 | 실물 반증 (이미 존재하는데 없다고 착각) | 통과 — `shellrc-doctor` 등 실물 확인 후 흡수 전략으로 전환 |

**G1·G2 주의:** 스코프를 일반 OS 설정이나 "개인비서"로 넓히면 **즉시 기각**이다. 이 경계는 협상 대상이 아니다.

### 현재 채점 추정 (인터뷰 이전 시점, 참고용)

인터뷰 직전 추정치는 **원점수 75/100**, 조정계수(R×S×L) 적용 시 **약 65~71점**으로 "기각~보류 경계"였다. 85점을 넘기기 위한 레버 3개는 모두 이 스펙에 반영되어 있다.

| 레버 | 반영 위치 |
|---|---|
| 골든셋 확보 → 활용성(2-2)·PT(2-1) 견인 | §10 |
| 실제 커뮤니티 접점 → 커뮤니티 확장(2-4) 견인 | §11 |
| fixture 기반 기능테스트 자동화 → 기능테스트(2-6) 견인 | §8 |

> 이 추정치는 **인터뷰 이전 값**이며 재계산하지 않았다. 이후 확정된 사항(1인 팀, Build(A) 전환, 어댑터 축소, 제출물 요건 반영)이 반영되지 않았으므로 **목표 설정의 참고로만 쓰고 보고서에는 절대 기재하지 않는다.**

### 2주 완주에 불리한 특성 (해당 항목만)

`archive/research/03-tech-trends-2026.md` — **채점 항목이 아니라 완주 리스크 요인이다.**

- **3개 이상의 배포 타깃** → 우리가 코어+펫앱 2개로 묶는 근거 (§4)
- 정확성 증명이 어려운 알고리즘 코어 → 해당 없음
- 모바일 네이티브 앱 → 해당 없음

유리한 특성 중 우리가 취한 것: **서버 인프라 0**(로컬 실행), **좁고 명확한 입출력**(fixture 테스트), **eval/골든셋을 프로젝트에 포함**(§10).

---

## 부록 B: 재논의 금지 항목

아래는 이전 세션과 이번 인터뷰에서 검토를 마치고 확정한 사항이다. 시간 낭비를 막기 위해 재논의하지 않는다.

- 온보딩 일회성 도구로 회귀 → **금지** (드리프트 감지가 핵심 가치)
- 트레이 아이콘 / 터미널 프롬프트 방식 → **금지** (온스크린 펫 확정)
- 실제 OS 접근성 API로 System Settings 조작 → **금지** (자체 토글 패널로 대체)
- 스코프를 일반 OS 설정·개인비서로 확장 → **금지** (하드 게이트 G1·G2 즉시 기각)
- OpenPets 플러그인 경로 → **금지** (§3에서 SDK 실사로 기각)
- Tauri / Rust → **금지** (§4)
- 헤드리스 에이전트 호출을 핵심 메커니즘으로 → **금지** (G3 위험)
